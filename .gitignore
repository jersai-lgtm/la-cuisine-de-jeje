# Dépendances et artefacts de build (régénérés par le workflow)
node_modules/
dist/

# État local de wrangler (Cloudflare Worker)
.wrangler/
worker/.wrangler/

# Dossier de dépôt des photos à convertir (outil tools/convertir-images.py)
_photos-a-convertir/

# === SÉCURITÉ : ne JAMAIS commiter de secrets ni les scripts batch (clé API) ===
# (le dépôt est public — tout fichier commité devient lisible sur github.com)
.env
.env.*
*.key
**/enrichir_batch.js
tools/traduire_batch.mjs
